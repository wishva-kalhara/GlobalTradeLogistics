# Desired state for the PostgreSQL container and the app's JDBC connection.
# Consumed natively by the postgres image and by .build-new/entrypoint.sh.
POSTGRES_DB=global_trade_log_corp
POSTGRES_USER=gtl_app
POSTGRES_PASSWORD=gtl_app
